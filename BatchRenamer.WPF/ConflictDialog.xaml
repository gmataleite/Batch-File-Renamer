<Window x:Class="BatchRenamer.WPF.ConflictDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Conflito de Arquivo" Height="200" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="20">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,20"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Substituir" Click="BtnReplace_Click" Width="100" Background="#007ACC" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Manter Ambos" Click="BtnKeepBoth_Click" Width="100" Background="#28A745" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="Pular" Click="BtnSkip_Click" Width="100" Background="#6C757D" Foreground="White"/>
        </StackPanel>
    </StackPanel>
</Window>
